{
  "name": "game-flanges-memory-matrix",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "build:host": "npm run build && node ./scripts/prepare-host-dist.mjs",
    "preview": "vite preview",
    "preview:host": "npm run build:host && vite preview --outDir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
